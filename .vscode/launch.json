{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387

    // Feed service
    // pm2 start ./service/feed_app.js --name Feed

   
    // #### 对于pm2来说，on前面的--不能少
    // pm2 start ./strategy/revTrend_strategy.js --name R01 -- on -a R01
    // pm2 start ./strategy/revTrend_strategy.js --name S01 -- on -a S01
    // pm2 start ./strategy/revTrend_strategy.js --name R06 -- on -a R06 
    // pm2 start ./strategy/revTrend_strategy.js --name R12 -- on -a R12
    // pm2 start ./strategy/revTrend_strategy.js --name R24 -- on -a R24
    // pm2 start ./strategy/revTrend_strategy.js --name R48 -- on -a R48
    // pm2 start ./strategy/simpleTrend_strategy.js --name STR -- on -a STR 
    // pm2 start ./strategy/simpleRevTrend_strategy.js --name SRE -- on -a SRE
    // pm2 start ./strategy/revTrend_xem_strategy.js --name XEM -- on -a XEM
    // pm2 start ./strategy/quickTrend_strategy.js --name Q01 -- on -a Q01
    // pm2 start ./strategy/quickTrend_strategy.js --name Q24 -- on -a Q24
    // pm2 start ./strategy/fundingRate_strategy.js --name FRA -- on -a FRA

    // pm2 start ./strategy/balance_monitor.js --name BAM

    // pm2 start announcement_monitor.py --name ANN --interpreter python3

    // #### UI 部分
    // cd middleware: pm2 start ./bin/www.js
    // 切换回~/jq_ui/，然后pm2 start npm --name "UI_React" -- start

    // node
    // node ./strategy/simpleRevTrend_strategy.js on -a SRE
    // node ./strategy/quickTrend_strategy.js on -a Q01
    "version": "0.2.0",
    "configurations": []
}